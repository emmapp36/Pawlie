PORT=4000
DATABASE_URL=postgres://pawlie:pawlie@localhost:5432/pawlie
CORS_ORIGIN=http://localhost:3000

# Chat generation. Without a key the API falls back to the scripted offline
# model (deterministic canned responses, no network).
ANTHROPIC_API_KEY=
# Optional override; defaults to claude-opus-4-8.
PAWLIE_CHAT_MODEL=
